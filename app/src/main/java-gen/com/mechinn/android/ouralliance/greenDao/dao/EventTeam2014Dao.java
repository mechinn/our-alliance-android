package com.mechinn.android.ouralliance.greenDao.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.mechinn.android.ouralliance.greenDao.Event2014;
import com.mechinn.android.ouralliance.greenDao.Team;

import com.mechinn.android.ouralliance.greenDao.EventTeam2014;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table EVENT_TEAM2014.
*/
public class EventTeam2014Dao extends AbstractDao<EventTeam2014, Long> {

    public static final String TABLENAME = "EVENT_TEAM2014";

    /**
     * Properties of entity EventTeam2014.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Modified = new Property(1, java.util.Date.class, "modified", false, "MODIFIED");
        public final static Property EventId = new Property(2, long.class, "EventId", false, "EVENT_ID");
        public final static Property TeamId = new Property(3, long.class, "teamId", false, "TEAM_ID");
        public final static Property Rank = new Property(4, Integer.class, "rank", false, "RANK");
        public final static Property Scouted = new Property(5, Boolean.class, "scouted", false, "SCOUTED");
    };

    private DaoSession daoSession;

    private Query<EventTeam2014> event2014_TeamsQuery;
    private Query<EventTeam2014> team_EventsQuery;

    public EventTeam2014Dao(DaoConfig config) {
        super(config);
    }
    
    public EventTeam2014Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'EVENT_TEAM2014' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'MODIFIED' INTEGER NOT NULL ," + // 1: modified
                "'EVENT_ID' INTEGER NOT NULL ," + // 2: EventId
                "'TEAM_ID' INTEGER NOT NULL ," + // 3: teamId
                "'RANK' INTEGER," + // 4: rank
                "'SCOUTED' INTEGER);"); // 5: scouted
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_EVENT_TEAM2014_EVENT_ID_TEAM_ID ON EVENT_TEAM2014" +
                " (EVENT_ID,TEAM_ID);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'EVENT_TEAM2014'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, EventTeam2014 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getModified().getTime());
        stmt.bindLong(3, entity.getEventId());
        stmt.bindLong(4, entity.getTeamId());
 
        Integer rank = entity.getRank();
        if (rank != null) {
            stmt.bindLong(5, rank);
        }
 
        Boolean scouted = entity.getScouted();
        if (scouted != null) {
            stmt.bindLong(6, scouted ? 1l: 0l);
        }
    }

    @Override
    protected void attachEntity(EventTeam2014 entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public EventTeam2014 readEntity(Cursor cursor, int offset) {
        EventTeam2014 entity = new EventTeam2014( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            new java.util.Date(cursor.getLong(offset + 1)), // modified
            cursor.getLong(offset + 2), // EventId
            cursor.getLong(offset + 3), // teamId
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // rank
            cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0 // scouted
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, EventTeam2014 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setModified(new java.util.Date(cursor.getLong(offset + 1)));
        entity.setEventId(cursor.getLong(offset + 2));
        entity.setTeamId(cursor.getLong(offset + 3));
        entity.setRank(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setScouted(cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0);
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(EventTeam2014 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(EventTeam2014 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "teams" to-many relationship of Event2014. */
    public List<EventTeam2014> _queryEvent2014_Teams(long EventId) {
        synchronized (this) {
            if (event2014_TeamsQuery == null) {
                QueryBuilder<EventTeam2014> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.EventId.eq(null));
                event2014_TeamsQuery = queryBuilder.build();
            }
        }
        Query<EventTeam2014> query = event2014_TeamsQuery.forCurrentThread();
        query.setParameter(0, EventId);
        return query.list();
    }

    /** Internal query to resolve the "events" to-many relationship of Team. */
    public List<EventTeam2014> _queryTeam_Events(long EventId) {
        synchronized (this) {
            if (team_EventsQuery == null) {
                QueryBuilder<EventTeam2014> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.EventId.eq(null));
                team_EventsQuery = queryBuilder.build();
            }
        }
        Query<EventTeam2014> query = team_EventsQuery.forCurrentThread();
        query.setParameter(0, EventId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getEvent2014Dao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getTeamDao().getAllColumns());
            builder.append(" FROM EVENT_TEAM2014 T");
            builder.append(" LEFT JOIN EVENT2014 T0 ON T.'EVENT_ID'=T0.'_id'");
            builder.append(" LEFT JOIN TEAM T1 ON T.'TEAM_ID'=T1.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected EventTeam2014 loadCurrentDeep(Cursor cursor, boolean lock) {
        EventTeam2014 entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Event2014 event2014 = loadCurrentOther(daoSession.getEvent2014Dao(), cursor, offset);
         if(event2014 != null) {
            entity.setEvent2014(event2014);
        }
        offset += daoSession.getEvent2014Dao().getAllColumns().length;

        Team team = loadCurrentOther(daoSession.getTeamDao(), cursor, offset);
         if(team != null) {
            entity.setTeam(team);
        }

        return entity;    
    }

    public EventTeam2014 loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<EventTeam2014> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<EventTeam2014> list = new ArrayList<EventTeam2014>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<EventTeam2014> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<EventTeam2014> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
